Given the following context:
Here, we consider the processed data of the executed method ${method}. It was called by ${caller} at ${annotatedAt}.
As a user I'd like to understand what happened inside this method.
For this purpose, a text-based explanation has to be generated from the provided data by summarizing all this information.

Method arguments (input):
${input}

${processedInformation}

Return values (output):
${output}